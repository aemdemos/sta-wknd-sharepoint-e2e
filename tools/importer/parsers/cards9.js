/* global WebImporter */
export default function parse(element, { document }) {
  // The HTML provided does NOT contain a 'Cards (cards9)' block.
  // As such, the correct behavior is to do NOTHING and leave the element unchanged.
}
